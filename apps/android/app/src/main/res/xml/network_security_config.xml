<?xml version="1.0" encoding="utf-8"?>
<!--
  apps/android/app/src/main/res/xml/network_security_config.xml — Task 7.3/7.4.
  Production talks https://api.mishran.app and needs no cleartext. The debug
  build's base URL is http://10.0.2.2:3000 (emulator → host loopback), so
  cleartext is permitted ONLY for the emulator loopback and localhost. Every
  other host inherits the platform default (cleartext denied on API 28+).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
