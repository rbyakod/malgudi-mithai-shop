//
// CartEstimate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Read-only pricing preview of a cart — exactly the /cart/validate math (server-side line pricing, tier fee, threshold waiver) but persisted nowhere and authenticated by nobody. Guest carts use it to show delivery fees before sign-in. Stale/unpriceable lines still error (PRODUCT_NOT_FOUND / VALIDATION) so callers can tell \&quot;estimate\&quot; from \&quot;your cart is broken\&quot;.  */
public struct CartEstimate: Codable, JSONEncodable, Hashable {

    public static let itemsTotalInPaiseRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let deliveryFeeInPaiseRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let discountInPaiseRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let totalInPaiseRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let freeDeliveryThresholdInPaiseRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var itemsTotalInPaise: Int
    public var deliveryFeeInPaise: Int
    public var discountInPaise: Int
    public var totalInPaise: Int
    /** Service tier of the resolved pincode; null when no/unserviceable pincode was sent. */
    public var pincodeTier: String?
    /** Tier's free-delivery threshold in paise; null when the tier is unknown. */
    public var freeDeliveryThresholdInPaise: Int?
    /** True when the tier is known and the subtotal met the threshold (fee already zeroed). */
    public var freeDeliveryEligible: Bool

    public init(itemsTotalInPaise: Int, deliveryFeeInPaise: Int, discountInPaise: Int, totalInPaise: Int, pincodeTier: String?, freeDeliveryThresholdInPaise: Int?, freeDeliveryEligible: Bool) {
        self.itemsTotalInPaise = itemsTotalInPaise
        self.deliveryFeeInPaise = deliveryFeeInPaise
        self.discountInPaise = discountInPaise
        self.totalInPaise = totalInPaise
        self.pincodeTier = pincodeTier
        self.freeDeliveryThresholdInPaise = freeDeliveryThresholdInPaise
        self.freeDeliveryEligible = freeDeliveryEligible
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case itemsTotalInPaise
        case deliveryFeeInPaise
        case discountInPaise
        case totalInPaise
        case pincodeTier
        case freeDeliveryThresholdInPaise
        case freeDeliveryEligible
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(itemsTotalInPaise, forKey: .itemsTotalInPaise)
        try container.encode(deliveryFeeInPaise, forKey: .deliveryFeeInPaise)
        try container.encode(discountInPaise, forKey: .discountInPaise)
        try container.encode(totalInPaise, forKey: .totalInPaise)
        try container.encode(pincodeTier, forKey: .pincodeTier)
        try container.encode(freeDeliveryThresholdInPaise, forKey: .freeDeliveryThresholdInPaise)
        try container.encode(freeDeliveryEligible, forKey: .freeDeliveryEligible)
    }
}

