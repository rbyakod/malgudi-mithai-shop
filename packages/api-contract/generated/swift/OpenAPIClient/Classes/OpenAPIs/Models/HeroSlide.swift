//
// HeroSlide.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One resolved slide of the admin-curated &#x60;home-hero&#x60; global — the same list the web hero renders. &#x60;vertical&#x60; + &#x60;slug&#x60; is the apps&#39; deep-link vocabulary (product detail for mithai, vertical detail otherwise). Draft products and products without an image or slug are dropped server-side; gift-boxes slides are skipped (no app surface — web routes them into /build-a-gift).  */
public struct HeroSlide: Codable, JSONEncodable, Hashable {

    public enum Vertical: String, Codable, CaseIterable {
        case mithai = "mithai"
        case qsr = "qsr"
        case snacks = "snacks"
        case merch = "merch"
    }
    /** Product id (unique within its collection). */
    public var id: String
    public var vertical: Vertical
    public var slug: String
    /** captionOverride when the admin set one, else product name. */
    public var name: String
    /** Mithai displayPrice / merch price; absent for other verticals. */
    public var priceLabel: String?
    public var imageURL: String
    public var imageAlt: String

    public init(id: String, vertical: Vertical, slug: String, name: String, priceLabel: String? = nil, imageURL: String, imageAlt: String) {
        self.id = id
        self.vertical = vertical
        self.slug = slug
        self.name = name
        self.priceLabel = priceLabel
        self.imageURL = imageURL
        self.imageAlt = imageAlt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case vertical
        case slug
        case name
        case priceLabel
        case imageURL
        case imageAlt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(vertical, forKey: .vertical)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(priceLabel, forKey: .priceLabel)
        try container.encode(imageURL, forKey: .imageURL)
        try container.encode(imageAlt, forKey: .imageAlt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension HeroSlide: Identifiable {}
